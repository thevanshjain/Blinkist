import React from 'react';
import Tab from './Tab';

export default {
    title: 'organism/tabs',
    component: Tab
}

const Template = (args) => <Tab {...args} />

export const TabBar = Template.bind({});
